# dependencies
/node_modules

# next.js
/.next
/out

# production
/build

# typescript
*.tsbuildinfo
next-env.d.ts

# local env & secrets
.env*.local

# local database
*.db
*.db-shm
*.db-wal

# personal resume assets (user-supplied, never committed)
/resume

# eval run output (session artifact; the eval set itself lives in golden/)
/scripts/eval-score-results.json

# os
.DS_Store
Thumbs.db
